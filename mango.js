class mangos  {
constructor(x, y,r){
    var options= {
    isSstatic:true,
    restitution:0,
    friction:1
    }
    display()
                var pos =this.body.position;
        ellipseMode(CENTER);
        fill("brown");
        rect(pos.x, pos.y, this.r,this.r);
    
}
}
